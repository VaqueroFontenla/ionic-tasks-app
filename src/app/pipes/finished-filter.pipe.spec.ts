import { FinishedFilterPipe } from './finished-filter.pipe';

describe('FinishedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FinishedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
